[project]
name = "cfi-synthgen"
version = "0.1.0"
requires-python = "==3.11.*"
dependencies = [
    "bpy==4.2.0",
    "numpy",
    "pillow",
    "pycocotools",
]

[project.optional-dependencies]
# pack_lmdb.py (UniTEX-FLUX LMDB packing): uv sync --extra lmdb
lmdb = ["lmdb"]

[tool.uv]
package = false
